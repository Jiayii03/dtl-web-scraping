## To run the setup_cron.sh

1. Create virtual environment
python3 -m venv venv

2. Activate the virtual environment
source venv/bin/activate

3. Install required packages
pip install -r requirements.txt

